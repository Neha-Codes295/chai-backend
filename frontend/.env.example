# Copy to .env only if you need overrides (optional in dev).

# Production build: API origin, no trailing slash.
# VITE_API_URL=https://api.example.com

# Dev: proxy target if the API is not on localhost:8001.
# VITE_DEV_API_PROXY=http://localhost:8001
