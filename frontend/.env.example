# Copy to .env and adjust. Real .env is gitignored.

# Production: API origin without trailing slash (leave empty in dev to use Vite /api proxy).
# VITE_API_URL=https://api.yoursite.com

# Dev only: backend URL for vite.config proxy when not using default port 8001.
# VITE_DEV_API_PROXY=http://localhost:8001
