user & video model with hooks & JWT

imp:
mongoose-aggregate-paginate-v2
allows aggregation query
isko export se phle use krna hota h 
for plugin injection

password & token to be discussed afterwards, for now we will just store them as strings. In production, we would hash the password and securely manage tokens.
bcrypt/ bcryptjs : helps to hash pswd
jwt: token

pre hook is used: data save hone se just phle run krv skte h\


JWT is a bearer token, who bears, usko data dena h


session & cookies both we're using , so very high security