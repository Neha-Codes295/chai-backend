aggregation pipelines for professionals

so many operations are available in mongoDB

[
    {}, //first pipeline stage
    {}, //second pipeline stage
    {} //third pipeline stage
]
values jo aati h , they are in array

subscribe button if not subscribed, elsee subscribed button
getUserChannelProfile in controllers