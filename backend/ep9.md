custom api response and error handling
more setup

imp:
req.body
req.params

install cookie-parser, cors


middleware: check middle resp
/insta: check(if user logged in)[could be multiple checks]:  ( req, res): res.send("neha");

{err,req,res,next}
next is flag

server status code